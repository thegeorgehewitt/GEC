#include <iostream>
#include <string>
using namespace std;

int main()
{
    string fullName;
    cout << "Please input a sentence." << endl;
    getline(cin, fullName);

    while (fullName.find("e") > 0)
    {
        
    }
}